{
  "name": "@bodhi/golden-master",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Golden-master fidelity harness: replays candidate answer sets through the scoring engine and diffs against legacy-produced expected outputs. Ships with synthetic fixtures; real fixtures land with the D-04 reference-data dump and legacy report samples (D-07).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@bodhi/scoring-engine": "*",
    "zod": "^3.24.1"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json"
  }
}
